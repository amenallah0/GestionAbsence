@model GAbsence.Models.Departement

@{
    ViewData["Title"] = "Ajouter un département";
}

<h1>@ViewData["Title"]</h1>

<div class="row">
    <div class="col-md-6">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group mb-3">
                <label asp-for="CodeDepartement" class="control-label">Code du département</label>
                <input asp-for="CodeDepartement" class="form-control" />
                <span asp-validation-for="CodeDepartement" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="NomDepartement" class="control-label">Nom du département</label>
                <input asp-for="NomDepartement" class="form-control" />
                <span asp-validation-for="NomDepartement" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Créer" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary">Retour à la liste</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} 